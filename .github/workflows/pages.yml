# Publishes docs/ as the one production GitHub Pages site from main only.
# GitHub Pages serves github.io over HTTPS. Do not retarget this workflow at a feature branch
# and do not add a second production site. PR #10 is merged; live URL:
# https://projectsoulbytmb.github.io/project---soul/
name: Deploy project website
on:
  push:
    branches: [main]
    paths: ['docs/**', '.github/workflows/pages.yml']
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: pages
  cancel-in-progress: true
jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/configure-pages@v6
      - uses: actions/upload-pages-artifact@v5
        with:
          path: docs
      - id: deployment
        uses: actions/deploy-pages@v5

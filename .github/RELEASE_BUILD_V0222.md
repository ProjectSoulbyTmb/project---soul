# v0.22.2 Windows release build trigger

This branch-only marker exists to trigger the verified Windows release workflow after the workflow gained a dedicated branch push trigger. It is not intended for merge to main.

[
    "WCAG contrast math hits canonical values and verdicts",
    "builder tier registers automatically as L0 tools",
    "bus dispatches allowed tools with capped, logged results",
    "catalog is frozen, unique, and read-class only",
    "code.metrics reports size, depth, functions, and detected smells",
    "entropy scales with pool and length; weak inputs earn warnings",
    "explain.tool generates usage docs; unknown ids list alternatives",
    "http probe returns metadata only - never body content - with timing",
    "jwt reader rejects malformed tokens and oversized input",
    "language tier registers as L0 and dispatches without any grant",
    "learning tier registers all five tools as L0 via the kernel",
    "live run with an L2 step is refused by preflight without admin gate",
    "manifest v2 validation accepts a correct contract and rejects drift",
    "routine define via command validates against the registry",
    "routine list starts empty and reflects definitions",
    "routine show prints steps; unknown id fails honestly",
    "routines validate limits, dry-run safely, and stop on failure by default",
    "self.patterns aggregates per-tool outcomes and breaker state",
    "self.portrait reflects the live registry exactly",
    "self.replay returns newest-first events after real dispatches",
    "standing grants authorize matching-or-lower classes and can expire",
    "usage and unknown ops fail with hints, never throw",
    "usage-theory tier registers as L0 and dispatches grant-free via intent"
]
